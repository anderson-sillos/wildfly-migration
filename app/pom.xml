<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>br.com.asillos.migration</groupId>
  <artifactId>wildfly-migration-lab</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <packaging>war</packaging>

  <name>WildFly Migration Lab</name>
  <description>Aplicação evolutiva do baseline legado ao destino Jakarta EE.</description>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    <project.build.outputTimestamp>2026-01-01T00:00:00Z</project.build.outputTimestamp>
    <migration.build.directory>${project.basedir}/target</migration.build.directory>
    <maven.compiler.release>17</maven.compiler.release>
    <java.version.range>[17,18)</java.version.range>

    <jakarta.ee.web.api.version>8.0.0</jakarta.ee.web.api.version>
    <mybatis.version>3.5.19</mybatis.version>
    <slf4j.version>2.0.18</slf4j.version>
    <xmlbeans.version>5.3.0</xmlbeans.version>
    <dom4j.version>2.2.0</dom4j.version>
  </properties>

  <dependencies>
    <!--
      O perfil padrão permanece como referência EE 8 para os gates históricos.
      O perfil cp-3e-jakarta11 fornece Jakarta EE 11 sem colocar APIs do
      servidor em WEB-INF/lib; o código web moderno é ativado no CP-3F.
    -->
    <dependency>
      <groupId>jakarta.platform</groupId>
      <artifactId>jakarta.jakartaee-web-api</artifactId>
      <version>${jakarta.ee.web.api.version}</version>
      <scope>provided</scope>
    </dependency>

    <!-- Dependências modernizadas de forma incremental no gate Java 17. -->
    <dependency>
      <groupId>org.mybatis</groupId>
      <artifactId>mybatis</artifactId>
      <version>${mybatis.version}</version>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-api</artifactId>
      <version>${slf4j.version}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.xmlbeans</groupId>
      <artifactId>xmlbeans</artifactId>
      <version>${xmlbeans.version}</version>
    </dependency>
    <dependency>
      <groupId>org.dom4j</groupId>
      <artifactId>dom4j</artifactId>
      <version>${dom4j.version}</version>
    </dependency>
  </dependencies>

  <build>
    <directory>${migration.build.directory}</directory>
    <finalName>wildfly-migration</finalName>
    <pluginManagement>
      <plugins>
        <!--
          O Maven CLI executa o XMLBeans normalmente. Este metadata adicional
          informa ao m2e/JDT como executar o mesmo goal no workspace do VS
          Code e registrar target/generated-sources como fonte compilável.
          O plugin fica somente em pluginManagement e não altera o WAR.
        -->
        <plugin>
          <groupId>org.eclipse.m2e</groupId>
          <artifactId>lifecycle-mapping</artifactId>
          <version>1.0.0</version>
          <configuration>
            <lifecycleMappingMetadata>
              <pluginExecutions>
                <pluginExecution>
                  <pluginExecutionFilter>
                    <groupId>org.apache.xmlbeans</groupId>
                    <artifactId>xmlbeans</artifactId>
                    <versionRange>[5.3.0,)</versionRange>
                    <goals>
                      <goal>compile</goal>
                    </goals>
                  </pluginExecutionFilter>
                  <action>
                    <execute>
                      <runOnConfiguration>true</runOnConfiguration>
                      <runOnIncremental>true</runOnIncremental>
                    </execute>
                  </action>
                </pluginExecution>
              </pluginExecutions>
            </lifecycleMappingMetadata>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <version>3.0.0-M3</version>
        <executions>
          <execution>
            <id>enforce-java17-toolchain</id>
            <goals>
              <goal>enforce</goal>
            </goals>
            <configuration>
              <rules>
                <requireMavenVersion>
                  <version>[3.9.16]</version>
                </requireMavenVersion>
                <requireJavaVersion>
                  <version>${java.version.range}</version>
                </requireJavaVersion>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.8.1</version>
        <configuration>
          <release>${maven.compiler.release}</release>
          <encoding>${project.build.sourceEncoding}</encoding>
          <showWarnings>true</showWarnings>
          <compilerArgs>
            <arg>-Xlint:all</arg>
            <arg>-Werror</arg>
          </compilerArgs>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <version>3.4.2</version>
        <executions>
          <execution>
            <id>validator-sci-jar</id>
            <phase>prepare-package</phase>
            <goals>
              <goal>jar</goal>
            </goals>
            <configuration>
              <classifier>validator-sci</classifier>
              <archive>
                <addMavenDescriptor>false</addMavenDescriptor>
              </archive>
              <includes>
                <include>br/com/asillos/migration/integration/validation/Validator.class</include>
                <include>br/com/asillos/migration/integration/validation/PedidoImportValidator.class</include>
                <include>br/com/asillos/migration/integration/validation/ValidatorDiscovery*.class</include>
                <include>br/com/asillos/migration/integration/validation/ValidatorServletContainerInitializer*.class</include>
                <include>META-INF/services/jakarta.servlet.ServletContainerInitializer</include>
              </includes>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-war-plugin</artifactId>
        <version>3.3.2</version>
        <configuration>
          <failOnMissingWebXml>true</failOnMissingWebXml>
          <outputTimestamp>${project.build.outputTimestamp}</outputTimestamp>
          <packagingExcludes>
            WEB-INF/classes/br/com/asillos/migration/integration/validation/Validator.class,
            WEB-INF/classes/br/com/asillos/migration/integration/validation/PedidoImportValidator.class,
            WEB-INF/classes/br/com/asillos/migration/integration/validation/ValidatorDiscovery*.class,
            WEB-INF/classes/br/com/asillos/migration/integration/validation/ValidatorServletContainerInitializer*.class,
            WEB-INF/classes/META-INF/services/jakarta.servlet.ServletContainerInitializer
          </packagingExcludes>
          <webResources>
            <resource>
              <directory>${project.build.directory}</directory>
              <includes>
                <include>${project.build.finalName}-validator-sci.jar</include>
              </includes>
              <targetPath>WEB-INF/lib</targetPath>
            </resource>
          </webResources>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <version>3.1.2</version>
      </plugin>
      <!--
        O XSD versionado é a fonte única dos tipos XMLBeans. O plugin 5.x
        regenera interfaces e o TypeSystemHolder em cada build; nada gerado
        deve ser mantido no controle de versão.
      -->
      <plugin>
        <groupId>org.apache.xmlbeans</groupId>
        <artifactId>xmlbeans</artifactId>
        <version>${xmlbeans.version}</version>
        <executions>
          <execution>
            <id>generate-pedido-xml-types</id>
            <goals>
              <goal>compile</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <sourceDir>${project.basedir}/src/main/resources/xsd</sourceDir>
          <sourceSchemas>pedido-importacao-v1.xsd</sourceSchemas>
          <javaTargetDir>${migration.build.directory}/generated-sources</javaTargetDir>
          <classTargetDir>${migration.build.directory}/generated-resources</classTargetDir>
          <name>PedidoImportacao</name>
          <repackage>br.com.asillos.migration.metadata</repackage>
          <sourceOnly>true</sourceOnly>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <id>oracle</id>
      <properties>
        <java.version.range>[17,18)</java.version.range>
      </properties>
    </profile>
    <profile>
      <id>ci-h2</id>
      <properties>
        <!-- O doctor fixa Temurin 17.0.20+8 nos dois perfis do CP-3A. -->
        <java.version.range>[17,18)</java.version.range>
      </properties>
    </profile>
    <profile>
      <id>cp-3e-jakarta11</id>
      <!--
        O JDT usa o JDK selecionado no workspace para importar os profiles.
        A ativação por JDK 21 mantém Jakarta ativo no editor sem alterar os
        gates históricos executados com Java 17.
      -->
      <activation>
        <jdk>[21,22)</jdk>
      </activation>
      <properties>
        <jakarta.ee.web.api.version>11.0.0</jakarta.ee.web.api.version>
        <maven.compiler.release>21</maven.compiler.release>
        <java.version.range>[21,22)</java.version.range>
      </properties>
    </profile>
  </profiles>
</project>
